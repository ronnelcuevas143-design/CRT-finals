name: CRT Auto Scan

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Check app
        run: |
          echo "Scan triggered at $(date)"
          curl -s -o /dev/null -w "%{http_code}" https://crt-finals.vercel.app
